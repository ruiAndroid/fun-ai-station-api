# Copy this file to `local.env` (or `.env`) and fill values.

# Server
APP_NAME=fun-ai-station-api
ENV=local
HOST=0.0.0.0
PORT=8001

# Database (MySQL)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=db_funaistation
DB_USER=root
DB_PASSWORD=your_password

# Auth
JWT_SECRET=change_me
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRES_MINUTES=60

# Agent service
FUN_AGENT_SERVICE_URL=http://localhost:4010
